page = PAGE
page {
    typeNum = 0
    shortcutIcon = EXT:firebrigade_theme/Resources/Public/Icons/favicon.ico

    10 = FLUIDTEMPLATE
    10 {
        templateName = Page
        extbase.controllerExtensionName = FirebrigadeTheme

        templateRootPaths {
            0 = EXT:firebrigade_theme/Resources/Private/Page/Templates/
            1 = {$firebrigade.page.fluidtemplate.templateRootPath}
        }
        partialRootPaths {
            0 = EXT:firebrigade_theme/Resources/Private/Page/Partials/
            1 = {$firebrigade.page.fluidtemplate.partialRootPath}
        }
        layoutRootPaths {
            0 = EXT:firebrigade_theme/Resources/Private/Page/Layouts/
            1 = {$firebrigade.page.fluidtemplate.layoutRootPath}
        }
        dataProcessing {
            10 = TYPO3\CMS\Frontend\DataProcessing\FilesProcessor
            10 {
                references.fieldName = media
                as = pageMedia
            }
            20 = TYPO3\CMS\Frontend\DataProcessing\MenuProcessor
            20 {
                levels = 2
                as = menuMain
            }
            25 = TYPO3\CMS\Frontend\DataProcessing\MenuProcessor
            25 {
                special = directory
                special.value = {$firebrigade.page.settings.pids.menu.footer}
                levels = 1
                as = menuFooter
            }
            30 = TYPO3\CMS\Frontend\DataProcessing\SiteProcessor
            30 {
                as = site
            }
        }
        variables {
            currentPageId = TEXT
            currentPageId.data = TSFE:id

            # Enable accessing BE Layout auf page/pageParents
            backendLayout = TEXT
            backendLayout.stdWrap {
                cObject = TEXT
                cObject {
                    data = levelfield:-2,backend_layout_next_level,slide
                    override.field = backend_layout
                    split {
                        token = pagets__
                        1.current = 1
                        1.wrap = |
                    }
                }
                ifEmpty = main
            }
            pageMediaIfEmpty = IMG_RESOURCE
            pageMediaIfEmpty {
                file = {$firebrigade.page.settings.pageMediaIfEmpty.path}
                stdWrap.if.isTrue = {$firebrigade.page.settings.pageMediaIfEmpty.path}
            }
        }
        settings {
            pageLogo {
                path = {$firebrigade.page.settings.pageLogo.path}
                alternative = {$firebrigade.page.settings.pageLogo.alternative}
                width = {$firebrigade.page.settings.pageLogo.width}
            }
            pageMedia {
                width = {$firebrigade.page.settings.pageMedia.width}
                height = {$firebrigade.page.settings.pageMedia.height}
            }
        }
    }

    meta {
        viewport = {$firebrigade.page.meta.viewport}
        robots = {$firebrigade.page.meta.robots}
        apple-mobile-web-app-capable = {$firebrigade.page.meta.apple-mobile-web-app-capable}
        description = {$firebrigade.page.meta.description}
        description {
            override.field = description
        }
        author = {$firebrigade.page.meta.author}
        author {
            override.field = author
        }
        keywords = {$firebrigade.page.meta.keywords}
        keywords {
            override.field = keywords
        }
        X-UA-Compatible = {$firebrigade.page.meta.compatible}
        X-UA-Compatible {
            attribute = http-equiv
        }

        # OpenGraph Tags
        og:title {
            attribute = property
            field = title
        }
        og:site_name {
            attribute = property
            data = TSFE:tmpl|setup|sitetitle
        }
        og:description = {$firebrigade.page.meta.description}
        og:description {
            attribute = property
            field = description
        }
        og:image {
            attribute = property
            stdWrap.cObject = FILES
            stdWrap.cObject {
                references {
                    data = levelfield:-1, media, slide
                }
                maxItems = 1
                renderObj = COA
                renderObj {
                    10 = IMG_RESOURCE
                    10 {
                        file {
                            import.data = file:current:uid
                            treatIdAsReference = 1
                            width = 1280c
                            height = 720c
                        }
                        stdWrap {
                            typolink {
                                parameter.data = TSFE:lastImgResourceInfo|3
                                returnLast = url
                                forceAbsoluteUrl = 1
                            }
                        }
                    }
                }
            }
        }
    }
}
#### Page Columns
######################
kanow.page.pageColumn < styles.content.get
kanow.page.pageColumn.select {
    where = colPos={field:column}
    where.insertData = 1
}

#### Check Column
######################
# check cols are not empty in fluid partial for columns
kanow.page.checkColumn = TEXT
kanow.page.checkColumn {
    value = 1
    if.isTrue {
        numRows = CONTENT
        numRows {
            table = tt_content
            select {
                where = colPos={field:column}
                where.insertData = 1
            }
        }
    }
}
